- 디바운싱
```
타이머를 5초안에 이벤트 여러개 발생(100개?) 할 때 그 때마다 사이즈의 레이아웃을 조정하게 되면 중간에 깨지거나 하기때문에..?
5초라고 정해놓으면 무수한 이벤트가 발생할 때 마다 5초 리셋
스타트 타이머 리셋
이벤트가 들어올때마다 타이머 재기동
max시간동안 이벤트가 안들어와야 실행
```

- 쓰로틀링
```
5초동안 이벤트를 여러번 호출했을 때 그 5초를 기다렸다가 마지막 이벤트만 실행
이벤트가 발생해도 5초까지는 기다림
5초에 마지막 한번은 무조건 발생
```